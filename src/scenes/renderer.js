/**
 * STUB — replaced by Agent B with the canvas scene renderer.
 * FROZEN CONTRACT.
 */

export const Scenes = {
  attach(_canvas) {},
  /** @param {'rain'|'embers'|'waves'|'stars'} _name */
  setScene(_name) {},
  setIntensity(_v) {},
  setNightMode(_on) {},
  pause() {},
  resume() {},
};
